/* ============================================================
   설정값 (config.js)
   - 이 파일에는 민감정보(API 주소, 토큰)를 절대 넣지 않는다.
   - 서버 URL·비밀 토큰은 api/register-visitor.js 안, 즉 서버에만 존재한다.
============================================================ */
'use strict';

const DEFAULT_PURPOSES = [
  '프로그램 참여',
  '행사운영 및 주최',
  '장비 및 시설이용(회의)',
  '비즈니스 미팅(투자/MOU)',
];

// 담당자 이메일은 이 도메인으로만 등록을 허용한다 (클라이언트 1차 검증용).
// 최종 검증은 항상 서버(api/register-visitor.js)에서 다시 수행한다.
const STAFF_EMAIL_DOMAIN = '@gbcommons.org';

// 메뉴 목록은 더 이상 하드코딩하지 않는다.
// 등록 폼에서는 더 이상 메뉴를 선택하지 않는다 — 실제 이용 메뉴는 대시보드
// (dashboard.html, js/dashboard.js)에서 /api/menu 를 호출해 구글시트
// '카페메뉴설정' 탭에서 실시간으로 가져와 입력한다.
